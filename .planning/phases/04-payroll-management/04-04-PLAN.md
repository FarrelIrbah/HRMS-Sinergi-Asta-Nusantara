---
phase: 04-payroll-management
plan: "04"
type: execute
wave: 4
depends_on: ["04-03"]
files_modified:
  - src/app/(dashboard)/payroll/page.tsx
  - src/app/(dashboard)/payroll/[periodId]/page.tsx
  - src/app/(dashboard)/payroll/[periodId]/_components/payroll-entry-table.tsx
  - src/app/(dashboard)/payroll/[periodId]/_components/finalize-button.tsx
  - src/app/(dashboard)/payroll/_components/run-payroll-form.tsx
autonomous: true

must_haves:
  truths:
    - "HR Admin can navigate to /payroll and see list of all payroll runs"
    - "HR Admin can select month/year and click 'Hitung Gaji' to trigger a payroll run"
    - "HR Admin can click a run to see all employee entries with gross, deductions, net pay"
    - "HR Admin can finalize a DRAFT payroll run with a confirm dialog"
    - "FINALIZED runs show a locked indicator; Hitung Gaji button is disabled for them"
  artifacts:
    - path: "src/app/(dashboard)/payroll/page.tsx"
      provides: "Payroll list page with run form"
      min_lines: 60
    - path: "src/app/(dashboard)/payroll/[periodId]/page.tsx"
      provides: "Payroll period detail page"
      min_lines: 50
    - path: "src/app/(dashboard)/payroll/[periodId]/_components/payroll-entry-table.tsx"
      provides: "DataTable of all employee payroll entries"
      min_lines: 60
    - path: "src/app/(dashboard)/payroll/[periodId]/_components/finalize-button.tsx"
      provides: "Finalize button with confirm dialog"
      min_lines: 40
  key_links:
    - from: "src/app/(dashboard)/payroll/page.tsx"
      to: "src/lib/actions/payroll.actions.ts"
      via: "calls runPayrollAction"
      pattern: "runPayrollAction"
    - from: "src/app/(dashboard)/payroll/[periodId]/_components/finalize-button.tsx"
      to: "src/lib/actions/payroll.actions.ts"
      via: "calls finalizePayrollAction"
      pattern: "finalizePayrollAction"
---

<objective>
Build the HR Admin payroll management UI: the payroll list page with a run form, the period detail page showing all employee entries, and the finalize button.

Purpose: This is the primary HR Admin workflow for PAY-01. The engine (Plan 03) is complete; this plan adds the UI layer on top.
Output: /payroll page with run form and payroll run list; /payroll/[periodId] with employee entry table and finalize action.
</objective>

<execution_context>
@C:\Users\farre\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\farre\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/04-payroll-management/04-03-SUMMARY.md
@src/lib/services/payroll.service.ts
@src/lib/actions/payroll.actions.ts
@src/app/(dashboard)/attendance-admin/page.tsx
@src/components/shared/DataTable.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Payroll list page with run-payroll form</name>
  <files>
    src/app/(dashboard)/payroll/page.tsx
    src/app/(dashboard)/payroll/_components/run-payroll-form.tsx
  </files>
  <action>
**src/app/(dashboard)/payroll/page.tsx** — server component:
- Auth check: requireHRAdmin() (redirect non-HR roles)
- Fetch: `const payrollRuns = await getPayrollRuns()` — returns list with id, month, year, status, entry count
- Render:
  - Page title "Penggajian" + breadcrumb
  - `<RunPayrollForm />` in a Card: "Hitung Penggajian Bulan Baru"
  - DataTable or simple table of payroll runs: columns Month/Year (formatted as "Januari 2026"), Status badge (DRAFT=amber, FINALIZED=green), Jumlah Karyawan, link to detail page
  - Empty state: "Belum ada data penggajian"

**src/app/(dashboard)/payroll/_components/run-payroll-form.tsx** — client component:
- React Hook Form + runPayrollSchema (month: 1-12, year)
- Two selects: Bulan (Januari-Desember) and Tahun (2024-2030)
- Submit button "Hitung Gaji" with loading spinner during submission
- On success: `router.push("/payroll/" + result.payrollRunId)` via useRouter
- On error: display error toast via sonner
- All labels in Indonesian (Bulan, Tahun, Hitung Gaji)
- Use shadcn/ui Select, Button, Card components
- Month display labels: ["Januari","Februari","Maret","April","Mei","Juni","Juli","Agustus","September","Oktober","November","Desember"]
  </action>
  <verify>
Navigate to /payroll — page loads without error.
Form renders with Bulan and Tahun selects.
Submit with month=1 year=2026 — runPayrollAction fires (check terminal for server log or DB entry).
  </verify>
  <done>
/payroll page loads. HR Admin can select month/year and submit the run form. Existing payroll runs appear in the list with correct status badges.
  </done>
</task>

<task type="auto">
  <name>Task 2: Payroll period detail page with entry table and finalize button</name>
  <files>
    src/app/(dashboard)/payroll/[periodId]/page.tsx
    src/app/(dashboard)/payroll/[periodId]/_components/payroll-entry-table.tsx
    src/app/(dashboard)/payroll/[periodId]/_components/finalize-button.tsx
  </files>
  <action>
**src/app/(dashboard)/payroll/[periodId]/page.tsx** — server component:
- Auth check: requireHRAdmin()
- Fetch: `const run = await getPayrollRunDetail(params.periodId)` — includes entries
- If run not found: return notFound()
- Render:
  - Breadcrumb: Penggajian > [Bulan Tahun]
  - Header row: period label + status badge + if DRAFT show `<FinalizeButton runId={run.id} />`
  - `<PayrollEntryTable entries={run.entries} />`
  - Summary footer: total gross, total deductions, total net for all employees

**src/app/(dashboard)/payroll/[periodId]/_components/payroll-entry-table.tsx** — client DataTable:
- Columns:
  - NIK (employeeNik)
  - Nama Karyawan (employeeName)
  - Gaji Pokok (baseSalary formatted Rp)
  - Tunjangan (totalAllowances formatted Rp)
  - Lembur (overtimePay formatted Rp)
  - THR (thrAmount formatted Rp, hidden if all zero)
  - Gaji Bruto (grossPay formatted Rp, bold)
  - Total Potongan (totalDeductions formatted Rp, red text)
  - Gaji Bersih (netPay formatted Rp, green text, bold)
  - Unduh Slip (link to /api/payroll/payslip/[entryId] — only works after FINALIZED, show disabled if DRAFT)
- All monetary values formatted as "Rp X.XXX.XXX" using Indonesian locale
- Searchable by employee name/NIK
- Accepts `runStatus` prop to conditionally enable/disable download links

**src/app/(dashboard)/payroll/[periodId]/_components/finalize-button.tsx** — client component:
- Button "Finalisasi Penggajian" (variant destructive or default with warning color)
- Opens ConfirmDialog (reuse existing shared component pattern): "Yakin ingin memfinalisasi penggajian ini? Data tidak dapat diubah setelah difinalisasi."
- On confirm: call finalizePayrollAction({ payrollRunId: runId }), show success toast, router.refresh()
- Shows loading spinner during submission
- Disabled state: if run is already FINALIZED
  </action>
  <verify>
Navigate to /payroll/[a-real-run-id] — page loads showing employee entries.
All monetary columns display Rp-formatted values.
FinalizeButton opens confirm dialog on click.
After finalize: status badge changes to "Finalisasi", button disappears.
  </verify>
  <done>
Period detail page loads with all employee entries. Monetary values formatted correctly. Finalize button triggers confirm dialog. After finalization, run shows FINALIZED status and button is no longer rendered.
  </done>
</task>

</tasks>

<verification>
1. /payroll page loads and shows existing payroll runs
2. Run form submits and redirects to period detail page
3. Period detail shows all employee entries with 9+ columns
4. Monetary values use Indonesian Rupiah formatting (Rp 5.000.000 with period separators)
5. Finalize button shows confirmation dialog before acting
6. After finalization: status = FINALIZED, finalize button hidden, download links active
</verification>

<success_criteria>
- PAY-01: HR Admin can initiate a monthly payroll run for all active employees
- DRAFT runs can be re-run (recalculated); FINALIZED runs are locked
- Entry table shows gross pay, BPJS breakdown, PPh 21, net pay per employee
- Finalize confirmation prevents accidental finalization
- All UI labels in Indonesian (Bulan, Hitung Gaji, Gaji Pokok, Gaji Bersih, Finalisasi, etc.)
</success_criteria>

<output>
After completion, create `.planning/phases/04-payroll-management/04-04-SUMMARY.md`
</output>
