---
phase: 05-recruitment-management
plan: "07"
type: execute
wave: 4
depends_on: ["05-04", "05-05", "05-06"]
files_modified:
  - src/components/layout/sidebar.tsx
  - src/lib/services/dashboard.service.ts
  - src/app/(dashboard)/dashboard/_components/hr-admin-dashboard.tsx
  - prisma/seed.ts
autonomous: false

must_haves:
  truths:
    - "Recruitment sidebar link appears for HR_ADMIN and SUPER_ADMIN roles"
    - "Dashboard openVacancies stat card shows the real count of BUKA vacancies"
    - "Seed data creates at least 2 vacancies, 3 candidates across stages, and 1 scheduled interview so the Kanban board is non-empty for demo"
    - "All 7 requirements (REC-01 through REC-07) can be demonstrated end-to-end in a single HR Admin session"
  artifacts:
    - path: "src/components/layout/sidebar.tsx"
      provides: "Recruitment nav item for HR_ADMIN/SUPER_ADMIN"
    - path: "src/lib/services/dashboard.service.ts"
      provides: "openVacancies field populated with real prisma.vacancy.count"
    - path: "prisma/seed.ts"
      provides: "Recruitment seed data (2 vacancies, 3+ candidates)"
  key_links:
    - from: "src/lib/services/dashboard.service.ts"
      to: "prisma.vacancy"
      via: "prisma.vacancy.count({ where: { status: 'BUKA' } })"
      pattern: "vacancy.count"
    - from: "src/app/(dashboard)/dashboard/_components/hr-admin-dashboard.tsx"
      to: "src/lib/services/dashboard.service"
      via: "openVacancies field from DashboardData"
      pattern: "openVacancies"
---

<objective>
Wire the recruitment module into the rest of the application: sidebar navigation, dashboard widget, seed data, and a human-verified end-to-end walkthrough.

Purpose: Without sidebar navigation, HR Admin cannot reach the module. Without seed data, the Kanban is empty and impossible to verify. This plan also serves as the final phase gate.
Output: Recruitment accessible from sidebar, dashboard shows open vacancy count, seed data provides demo content, and E2E verified by human.
</objective>

<execution_context>
@C:\Users\farre\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\farre\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/05-recruitment-management/05-CONTEXT.md
@.planning/phases/05-recruitment-management/05-04-SUMMARY.md
@.planning/phases/05-recruitment-management/05-05-SUMMARY.md
@.planning/phases/05-recruitment-management/05-06-SUMMARY.md
@src/components/layout/sidebar.tsx
@src/lib/services/dashboard.service.ts
@src/app/(dashboard)/dashboard/_components/hr-admin-dashboard.tsx
@prisma/seed.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Sidebar nav, dashboard widget, and seed data</name>
  <files>src/components/layout/sidebar.tsx, src/lib/services/dashboard.service.ts, src/app/(dashboard)/dashboard/_components/hr-admin-dashboard.tsx, prisma/seed.ts</files>
  <action>
**src/components/layout/sidebar.tsx** — add Recruitment nav item:
- Add `Briefcase` to lucide-react imports (already imported in hr-admin-dashboard — confirm it's in sidebar or add).
- Add nav item after the "Hitung THR" item and before "Slip Gaji":
```typescript
{
  label: "Rekrutmen",
  href: "/recruitment",
  icon: Briefcase,
  roles: ["SUPER_ADMIN", "HR_ADMIN"],
},
```

**src/lib/services/dashboard.service.ts** — replace `openVacancies: 0` placeholder with real query:
- Add `prisma.vacancy.count({ where: { status: "BUKA" } })` to the `Promise.all` array.
- Update the return object: `openVacancies: openVacanciesCount` (using the destructured result).

**src/app/(dashboard)/dashboard/_components/hr-admin-dashboard.tsx** — update if openVacancies widget exists as a placeholder. Find the StatCard with title "Lowongan Terbuka" (or similar) and ensure it uses `data.openVacancies` from the DashboardData. If the widget doesn't exist yet, add a new StatCard:
```tsx
<StatCard
  title="Lowongan Terbuka"
  value={data.openVacancies}
  description="Posisi aktif tersedia"
  icon={Briefcase}
/>
```
Import `Briefcase` from lucide-react if not already imported.

**prisma/seed.ts** — add recruitment seed data with idempotent guards (findFirst before create — project pattern):

Seed creates:
1. Two vacancies: "Software Engineer" (Departemen Teknologi Informasi, status BUKA) and "HR Specialist" (Departemen HRD, status BUKA). Use `createdBy` = the SUPER_ADMIN user's id found via `prisma.user.findFirst({ where: { role: "SUPER_ADMIN" } })`. Set openDate to new Date(). Use `findFirst`-before-create on vacancy title to avoid duplicates.

2. For the "Software Engineer" vacancy, create 3 candidates:
   - "Budi Santoso" — stage MELAMAR
   - "Siti Rahayu" — stage INTERVIEW, with an Interview record: `scheduledAt: new Date(Date.now() + 2 * 24 * 60 * 60 * 1000)` (2 days from now), interviewerId = SUPER_ADMIN userId, notes: "Interview teknis via Google Meet"
   - "Ahmad Fauzi" — stage DITERIMA, with offeredSalary: 8000000, startDate: new Date(Date.now() + 14 * 24 * 60 * 60 * 1000)

3. For the "HR Specialist" vacancy, create 1 candidate: "Dewi Kusuma" — stage SELEKSI_BERKAS.

Find the department IDs by querying `prisma.department.findFirst({ where: { name: { contains: "Teknologi" } } })` and `prisma.department.findFirst({ where: { name: { contains: "HRD" } } })`. If departments don't exist (names may differ in actual seed), fall back to any first active department.

Then run seed:
```bash
npx prisma db seed
```
  </action>
  <verify>
`npm run dev`. Log in as HR Admin. Sidebar shows "Rekrutmen" link. Click it — /recruitment page loads with seeded vacancies. Dashboard shows openVacancies count > 0.

Visit /recruitment/[softwareEngineerVacancyId]. Kanban board shows 3 candidates in correct columns (Melamar, Interview, Diterima).
  </verify>
  <done>Sidebar has Rekrutmen link. Dashboard shows real open vacancy count. Seed data creates 2 vacancies and 4 candidates across different stages. Kanban board non-empty for demo.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
Complete Phase 5 Recruitment Management module:
- Vacancy list page (/recruitment) with status filter and toggle
- Create vacancy form (/recruitment/new)
- Vacancy detail with drag-and-drop Kanban board (6 stage columns)
- Add candidate dialog on vacancy detail
- Candidate detail page with CV upload, interview scheduling, offer fields
- Offer letter PDF download for DITERIMA candidates
- Convert candidate to employee with pre-filled /employees/new form and CV attachment
- Sidebar nav and dashboard widget
- Seed data with 2 vacancies and 4 candidates
  </what-built>
  <how-to-verify>
Login as HR Admin and test these flows in order:

**REC-01 + REC-02: Vacancy management**
1. Click "Rekrutmen" in sidebar → /recruitment loads, shows seeded vacancies
2. Click "Buka Lowongan Baru" → fill form (title, department, description, open date) → submit → new vacancy in list
3. Toggle a vacancy status to "Tutup" → badge changes to Tutup → toggle back to Buka

**REC-03 + REC-04: Candidates and pipeline**
4. Click "Lihat Detail" on a vacancy → Kanban board shows columns with candidates
5. Drag a candidate card from one column to another → refresh page → card remains in new column
6. Click "Tambah Kandidat" → fill name + email → submit → new card in Melamar column

**REC-05: Interview scheduling**
7. Click on a candidate card → candidate detail page loads
8. Fill interview date, select interviewer, add notes → submit → interview section shows scheduled date; candidate stage moves to Interview

**REC-06: Offer letter**
9. Drag a candidate to Diterima stage (or use existing "Ahmad Fauzi" seed)
10. On candidate detail, fill offer salary + start date in the Offer section → save
11. Click "Download Surat Penawaran" → PDF downloads → open and verify: formal Indonesian letter with correct name, position, salary, dates, company name, HR admin signature block

**REC-07: Candidate-to-employee conversion**
12. On a Diterima candidate → click "Konversi ke Karyawan" → /employees/new opens with name, email, department pre-filled
13. Complete the required fields (contract type, join date, etc.) → submit → redirects to employee profile
14. Navigate back to the candidate → shows "Terkonversi" badge, no "Konversi" button
15. Check employee profile → Documents tab shows CV as LAINNYA document (if candidate had a CV)

**Dashboard**
16. Go to /dashboard → "Lowongan Terbuka" stat card shows count of open vacancies
  </how-to-verify>
  <resume-signal>Type "approved" if all 16 steps pass, or describe any issues found</resume-signal>
</task>

</tasks>

<verification>
Human approval required. All 7 REC requirements verified end-to-end:
- REC-01: Create vacancy with all fields
- REC-02: Toggle vacancy status
- REC-03: Add candidate with CV upload
- REC-04: Move candidate between pipeline stages via Kanban drag-and-drop
- REC-05: Schedule interview with date, interviewer, notes
- REC-06: Download offer letter PDF
- REC-07: Convert to employee with pre-fill and CV attachment
</verification>

<success_criteria>
- Recruitment sidebar link visible to HR_ADMIN and SUPER_ADMIN
- Dashboard openVacancies shows real vacancy count (not 0 placeholder)
- Seed data runs idempotently — running twice does not create duplicates
- All 7 REC requirements pass human verification
- Phase 5 complete
</success_criteria>

<output>
After completion, create `.planning/phases/05-recruitment-management/05-07-SUMMARY.md`
</output>
