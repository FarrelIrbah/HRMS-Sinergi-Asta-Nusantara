---
phase: 05-recruitment-management
plan: "03"
type: execute
wave: 2
depends_on: ["05-01", "05-02"]
files_modified:
  - src/app/(dashboard)/recruitment/page.tsx
  - src/app/(dashboard)/recruitment/new/page.tsx
  - src/app/(dashboard)/recruitment/new/_components/create-vacancy-form.tsx
autonomous: true

must_haves:
  truths:
    - "Navigating to /recruitment shows a table of all vacancies with title, department, candidate count, status badge, open date, and links to detail pages"
    - "HR Admin can filter the vacancy list by status (BUKA / TUTUP)"
    - "HR Admin can navigate to /recruitment/new, fill the vacancy form, submit, and be redirected to /recruitment"
    - "HR Admin can toggle vacancy status (BUKA ↔ TUTUP) from the vacancy list row without leaving the page"
  artifacts:
    - path: "src/app/(dashboard)/recruitment/page.tsx"
      provides: "Vacancy list server component with status filter and action buttons"
      min_lines: 40
    - path: "src/app/(dashboard)/recruitment/new/page.tsx"
      provides: "Create vacancy page (server component shell)"
    - path: "src/app/(dashboard)/recruitment/new/_components/create-vacancy-form.tsx"
      provides: "Client form for creating a vacancy (react-hook-form + zod)"
  key_links:
    - from: "src/app/(dashboard)/recruitment/page.tsx"
      to: "src/lib/services/recruitment.service"
      via: "getVacancies() called in server component"
      pattern: "getVacancies"
    - from: "src/app/(dashboard)/recruitment/new/_components/create-vacancy-form.tsx"
      to: "src/lib/actions/recruitment.actions"
      via: "createVacancyAction called on form submit"
      pattern: "createVacancyAction"
---

<objective>
Build the vacancy list page (with status filter and toggle) and the create vacancy form. These are the entry points to the recruitment module.

Purpose: HR Admin needs to see all vacancies and create new ones before any other recruitment flow can proceed.
Output: /recruitment page with filterable table and /recruitment/new with working create form.
</objective>

<execution_context>
@C:\Users\farre\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\farre\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/05-recruitment-management/05-CONTEXT.md
@.planning/phases/05-recruitment-management/05-01-SUMMARY.md
@.planning/phases/05-recruitment-management/05-02-SUMMARY.md
@src/app/(dashboard)/employees/page.tsx
@src/app/(dashboard)/employees/new/page.tsx
@src/lib/services/recruitment.service.ts
@src/lib/actions/recruitment.actions.ts
@src/lib/validations/recruitment.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Vacancy list page with status filter and toggle</name>
  <files>src/app/(dashboard)/recruitment/page.tsx</files>
  <action>
Create `src/app/(dashboard)/recruitment/page.tsx` as a server component. Mirror the pattern of `src/app/(dashboard)/employees/page.tsx`.

Auth: Redirect to /login if no session. All roles can see vacancies (read-only for non-HR). Only HR_ADMIN and SUPER_ADMIN see the "Buka Lowongan Baru" button and status toggle.

Accept `searchParams: Promise<{ status?: string }>`. Await searchParams. If `status === "BUKA"` or `status === "TUTUP"`, pass to `getVacancies(statusFilter)`. Otherwise pass undefined (show all).

Render:
- Page header: "Rekrutmen" title + "Buka Lowongan Baru" button (Link to /recruitment/new, visible to HR_ADMIN/SUPER_ADMIN only)
- Status filter tabs or buttons: "Semua", "Buka", "Tutup" — each is a Link with ?status= param. Active state highlighted.
- Table with columns: Posisi/Jabatan, Departemen, Jumlah Kandidat, Status (badge: green for BUKA, gray for TUTUP), Tanggal Buka, Aksi
- Aksi column: "Lihat Detail" button (Link to /recruitment/[vacancyId]). For HR_ADMIN/SUPER_ADMIN: inline status toggle button using a form with updateVacancyStatusAction (shows "Tutup Lowongan" if BUKA, "Buka Kembali" if TUTUP). Use a `<form>` with hidden inputs and a submit button for the server action call (no client-side JS needed).
- If no vacancies: empty state message "Belum ada lowongan. Klik 'Buka Lowongan Baru' untuk memulai."

Status badge colors: BUKA = `bg-green-100 text-green-800`, TUTUP = `bg-gray-100 text-gray-700`.

Import `updateVacancyStatusAction` from `@/lib/actions/recruitment.actions`. The status toggle form: POST to action, pass vacancyId and opposite status as hidden inputs wrapped in a `<form action={updateVacancyStatusAction}>` with bind — use `updateVacancyStatusAction.bind(null, { vacancyId: v.id, status: v.status === "BUKA" ? "TUTUP" : "BUKA" })` as the form action. Actually simpler: wrap in a small `<form>` and use a Server Action inline or import the bound action. The cleanest: make a client component `VacancyStatusToggle` that calls the action via `useTransition` and `startTransition`.

Create `src/app/(dashboard)/recruitment/_components/vacancy-status-toggle.tsx` as a "use client" component that accepts `{ vacancyId, currentStatus }`, calls `updateVacancyStatusAction({ vacancyId, status: opposite })` via startTransition, shows loading state during transition.
  </action>
  <verify>
Visit http://localhost:3000/recruitment (after `npm run dev`). Table renders. Status filter links work. "Buka Lowongan Baru" button is visible when logged in as HR_ADMIN.
  </verify>
  <done>Vacancy list page renders at /recruitment with table, status filter, and status toggle buttons. Empty state shown when no vacancies exist.</done>
</task>

<task type="auto">
  <name>Task 2: Create vacancy form page</name>
  <files>src/app/(dashboard)/recruitment/new/page.tsx, src/app/(dashboard)/recruitment/new/_components/create-vacancy-form.tsx</files>
  <action>
**src/app/(dashboard)/recruitment/new/page.tsx** — server component:
- Auth: redirect to /login if no session. Redirect to /recruitment if role is not HR_ADMIN or SUPER_ADMIN.
- Fetch: `getAllDepartments()` and `getAllPositions()` from `@/lib/services/master-data.service` (same import as employees/new).
- Render: page header "Buka Lowongan Baru" + CreateVacancyForm component passing departments and positions.

**src/app/(dashboard)/recruitment/new/_components/create-vacancy-form.tsx** — "use client":
- Props: `departments: { id: string; name: string }[]`, `positions: { id: string; name: string; departmentId: string }[]`
- Form fields:
  - `title`: text input, required, label "Posisi/Jabatan"
  - `departmentId`: Select dropdown with department options, required, label "Departemen". On change, filter positions shown in positionId dropdown.
  - `positionId`: Select dropdown filtered by selected department, optional, label "Posisi (opsional)"
  - `openDate`: date input (`<input type="date">`), required, label "Tanggal Buka"
  - `description`: Textarea, optional, label "Deskripsi Pekerjaan"
  - `requirements`: Textarea, optional, label "Persyaratan"
- Use react-hook-form with zodResolver(createVacancySchema). Import createVacancySchema from `@/lib/validations/recruitment`.
- Submit: call `createVacancyAction(data)`. If `result.success`, `router.push('/recruitment')` and `toast.success("Lowongan berhasil dibuat")`. If error, `toast.error(result.error)`.
- Submit button: "Buat Lowongan", disabled during submission. Back button: Link to /recruitment.
- Use shadcn Card, CardContent, Label, Input, Textarea, Button, Select components (check existing imports in create-employee-form for patterns).
  </action>
  <verify>
Navigate to /recruitment/new. Form renders with all fields. Submit with valid data. Redirects to /recruitment. New vacancy appears in list.
  </verify>
  <done>Create vacancy form at /recruitment/new submits successfully and vacancy appears in the list page. Department selection filters available positions.</done>
</task>

</tasks>

<verification>
1. GET /recruitment returns 200, shows vacancy table
2. GET /recruitment?status=BUKA filters to open vacancies only
3. GET /recruitment/new returns 200, form renders
4. POST create vacancy via form -> redirect to /recruitment -> vacancy in list
5. Status toggle changes vacancy status and table updates
</verification>

<success_criteria>
- /recruitment page renders vacancy table with department name, candidate count, status badge
- Status filter (Semua / Buka / Tutup) works via URL searchParams
- Create vacancy form validates, submits, redirects
- Status toggle works without full page reload (via useTransition)
- Non-HR roles can view the list but see no create/toggle buttons
</success_criteria>

<output>
After completion, create `.planning/phases/05-recruitment-management/05-03-SUMMARY.md`
</output>
