---
phase: 02-employee-data-management
plan: 05
type: execute
wave: 3
depends_on: ["02-02"]
files_modified:
  - src/app/(dashboard)/employees/new/page.tsx
  - src/app/(dashboard)/employees/new/_components/create-employee-form.tsx
autonomous: true

must_haves:
  truths:
    - "HR Admin can create a new employee with minimum required fields"
    - "Creating an employee automatically creates a User account with Employee role"
    - "After successful creation, user is redirected to the new employee's profile"
    - "Form validates all required fields before submission"
  artifacts:
    - path: "src/app/(dashboard)/employees/new/page.tsx"
      provides: "Create employee page (HR_ADMIN only)"
      min_lines: 15
    - path: "src/app/(dashboard)/employees/new/_components/create-employee-form.tsx"
      provides: "Employee creation form component"
      min_lines: 80
  key_links:
    - from: "src/app/(dashboard)/employees/new/_components/create-employee-form.tsx"
      to: "src/lib/actions/employee.actions.ts"
      via: "form onSubmit calls createEmployeeAction"
      pattern: "createEmployeeAction"
---

<objective>
Build the employee creation page and form.

Purpose: This is the entry point for adding new employees (EMP-06). HR Admin fills in the minimum required fields (name, email, password, department, position, contract type, join date) plus optional personal/tax fields. The form calls createEmployeeAction which atomically creates Employee + User. On success, redirects to the new employee profile.
Output: /employees/new page with a validated creation form.
</objective>

<execution_context>
@C:\Users\farre\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\farre\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/02-employee-data-management/02-CONTEXT.md
@.planning/phases/02-employee-data-management/02-RESEARCH.md

@src/app/(dashboard)/users/_components/user-form-dialog.tsx
@src/app/(dashboard)/master-data/_components/department-form-dialog.tsx
@src/lib/validations/employee.ts
@src/lib/actions/employee.actions.ts
@src/lib/services/master-data.service.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create employee creation page and form</name>
  <files>src/app/(dashboard)/employees/new/page.tsx, src/app/(dashboard)/employees/new/_components/create-employee-form.tsx</files>
  <action>
1. Create `src/app/(dashboard)/employees/new/page.tsx` (server component):
   - Auth check: redirect to /login if no session, redirect to /employees if not HR_ADMIN/SUPER_ADMIN
   - Fetch departments (getAllDepartments) and positions (from master-data service) for select dropdowns
   - Render page header "Tambah Karyawan" with breadcrumb or back link to /employees
   - Render CreateEmployeeForm component passing departments and positions

2. Create `src/app/(dashboard)/employees/new/_components/create-employee-form.tsx` ("use client"):
   - Use react-hook-form with zodResolver(createEmployeeSchema)
   - Form layout: use Card component with sections (not full tabbed form -- creation is a single form)
   - Sections within the card:

     **Informasi Akun** (Account Info):
     - Email (text input, required)
     - Password Awal (password input, required, with show/hide toggle)

     **Informasi Pribadi** (Personal Info):
     - Nama Lengkap (text input, required)
     - NIK KTP (text input, optional, maxLength 16)
     - Tempat Lahir (text input, optional)
     - Tanggal Lahir (date picker, optional -- use a simple input type="date" or the existing date picker pattern)
     - Jenis Kelamin (select: Laki-laki/Perempuan, optional)
     - Status Pernikahan (select: Tidak Kawin/Kawin, optional)
     - Agama (select from Religion enum, optional)
     - Alamat (textarea, optional)
     - Nomor HP (text input, optional)

     **Detail Pekerjaan** (Employment Details):
     - Departemen (select from departments prop, required)
     - Jabatan (select from positions prop -- filtered by selected department if positions are department-scoped, required)
     - Tipe Kontrak (select: PKWT/PKWTT, required)
     - Tanggal Masuk (date input, required)

     **Pajak & BPJS** (optional section):
     - NPWP (text input, optional)
     - Status PTKP (select from PTKPStatus values, optional)
     - No. BPJS Kesehatan (text input, optional)
     - No. BPJS Ketenagakerjaan (text input, optional)

   - Submit button "Simpan Karyawan" with loading state
   - On submit: call createEmployeeAction(formData)
   - On success: toast success message, router.push(`/employees/${result.data.id}`)
   - On error: toast error message from result.error
   - Cancel button links back to /employees

   Use existing UI components: Input, Select (from shadcn), Button, Card, Label, Textarea.
   Follow the same form pattern as user-form-dialog.tsx but as a full page form (not dialog).
   Use sonner toast for success/error notifications.
  </action>
  <verify>
Run `npx tsc --noEmit` -- no type errors.
Run `npm run build` -- page should compile.
  </verify>
  <done>Create employee form renders all fields organized in sections, validates with Zod, calls createEmployeeAction on submit, shows toast on success/error, and redirects to the new employee profile on success.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes
- /employees/new page is accessible only to HR_ADMIN/SUPER_ADMIN
- Form shows all required and optional fields organized in logical sections
- Validation errors display inline (react-hook-form)
- Successful submission creates Employee + User and redirects to profile
</verification>

<success_criteria>
1. HR_ADMIN can access /employees/new, other roles are redirected away
2. Form has all fields from EMP-01, EMP-02, EMP-03 organized in sections
3. Required fields: name, email, password, department, position, contract type, join date
4. Optional fields: all personal info, tax/BPJS info
5. Form validates before submission (inline errors in Indonesian)
6. Success: toast + redirect to /employees/{newId}
7. Error: toast with error message
</success_criteria>

<output>
After completion, create `.planning/phases/02-employee-data-management/02-05-SUMMARY.md`
</output>
