---
phase: 02-employee-data-management
plan: 08
type: execute
wave: 5
depends_on: ["02-06", "02-07"]
files_modified:
  - src/app/(dashboard)/employees/[id]/_components/deactivate-employee-dialog.tsx
  - src/app/(dashboard)/employees/[id]/page.tsx
  - prisma/seed.ts
  - src/app/(dashboard)/dashboard/_components/hr-admin-dashboard.tsx
  - src/app/(dashboard)/dashboard/_components/manager-dashboard.tsx
  - src/app/(dashboard)/dashboard/_components/employee-dashboard.tsx
autonomous: false

must_haves:
  truths:
    - "HR Admin can deactivate an employee with termination date and reason"
    - "Deactivated employee's User account is also deactivated (cannot log in)"
    - "Deactivated employees do not appear in active employee lists"
    - "Seed data includes sample employees for testing all roles"
    - "All three role dashboards link to the employee section"
  artifacts:
    - path: "src/app/(dashboard)/employees/[id]/_components/deactivate-employee-dialog.tsx"
      provides: "Deactivation dialog with termination date and reason"
      min_lines: 40
    - path: "prisma/seed.ts"
      provides: "Updated seed with sample employees"
      contains: "employee"
  key_links:
    - from: "src/app/(dashboard)/employees/[id]/_components/deactivate-employee-dialog.tsx"
      to: "src/lib/actions/employee.actions.ts"
      via: "dialog calls deactivateEmployeeAction"
      pattern: "deactivateEmployeeAction"
    - from: "src/app/(dashboard)/employees/[id]/page.tsx"
      to: "deactivate-employee-dialog.tsx"
      via: "page renders deactivate button for active employees"
      pattern: "DeactivateEmployeeDialog"
---

<objective>
Build employee deactivation flow, update seed data with sample employees, update role dashboards, and verify the complete Phase 2 integration.

Purpose: This is the final integration plan. It adds the deactivation feature (EMP-08), creates test data so all features can be verified, and updates the three role dashboards to link into the employee section. The checkpoint at the end verifies the entire phase works end-to-end.
Output: Complete employee data management system with deactivation, seed data, and dashboard integration.
</objective>

<execution_context>
@C:\Users\farre\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\farre\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/02-employee-data-management/02-CONTEXT.md

@src/app/(dashboard)/employees/[id]/page.tsx
@src/lib/actions/employee.actions.ts
@src/lib/validations/employee.ts
@prisma/seed.ts
@src/app/(dashboard)/dashboard/_components/hr-admin-dashboard.tsx
@src/app/(dashboard)/dashboard/_components/manager-dashboard.tsx
@src/app/(dashboard)/dashboard/_components/employee-dashboard.tsx
@src/components/shared/confirm-dialog.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build deactivation dialog, update seed data and dashboards</name>
  <files>src/app/(dashboard)/employees/[id]/_components/deactivate-employee-dialog.tsx, src/app/(dashboard)/employees/[id]/page.tsx, prisma/seed.ts, src/app/(dashboard)/dashboard/_components/hr-admin-dashboard.tsx, src/app/(dashboard)/dashboard/_components/manager-dashboard.tsx, src/app/(dashboard)/dashboard/_components/employee-dashboard.tsx</files>
  <action>
1. Create `deactivate-employee-dialog.tsx` ("use client"):
   - Trigger: "Nonaktifkan Karyawan" button (red/destructive variant)
   - Dialog content:
     - Warning text: "Karyawan yang dinonaktifkan tidak dapat login dan tidak akan muncul di daftar karyawan aktif."
     - Form fields:
       - Tanggal Pemberhentian (date input, required)
       - Alasan Pemberhentian (textarea, required)
     - Use react-hook-form with deactivateEmployeeSchema
   - On submit: call deactivateEmployeeAction(employeeId, formData)
   - On success: toast("Karyawan berhasil dinonaktifkan"), router.refresh()
   - On error: toast.error(result.error)
   - Use existing Dialog component from shadcn/ui

2. Update `employees/[id]/page.tsx`:
   - In the page header area, add "Nonaktifkan Karyawan" button (only for HR_ADMIN/SUPER_ADMIN, only when employee.isActive === true)
   - Render DeactivateEmployeeDialog component, passing employeeId
   - When employee is already inactive, show status banner "Karyawan ini sudah tidak aktif sejak {terminationDate}" with terminationReason below it

3. Update `prisma/seed.ts`:
   - Keep existing seed data (users, departments, positions, office locations)
   - Add Employee records for the 3 existing test users:
     - Link HR Admin user to an Employee record (department: "Human Resources" or first dept)
     - Link Manager user to an Employee record (department: pick one, e.g., "IT" or second dept)
     - Link Employee user to an Employee record (same department as manager, so manager can see them)
   - Add 5-7 more employees with varied data:
     - Mix of departments, positions, contract types (PKWT/PKWTT)
     - Some with tax/BPJS info filled, some without
     - 1 inactive/terminated employee for testing filter
     - Varied gender, religion, marital status
   - Add 1-2 emergency contacts for some employees
   - Generate NIKs following EMP-2026-NNNN format
   - Use bcryptjs for password hashing (same as existing seed pattern)
   - Ensure seed is idempotent (check if employees exist before creating, or use upsert)

4. Update dashboard stubs to link to employee section:
   - `hr-admin-dashboard.tsx`: Add "Karyawan" card with employee count and link to /employees
   - `manager-dashboard.tsx`: Add "Karyawan Departemen" card with department employee count and link to /employees
   - `employee-dashboard.tsx`: Add "Profil Saya" card with link to /employees (which redirects to their profile)
   - Follow existing dashboard card patterns (use Card, CardHeader, CardContent from shadcn)
  </action>
  <verify>
Run `npx tsc --noEmit` -- no type errors.
Run `npx prisma db seed` -- seed completes without errors.
Run `npm run build` -- full build passes.
  </verify>
  <done>Deactivation dialog works with form validation. Seed data includes 8-10 employees with varied data. Dashboard stubs link to employee section.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Complete Employee Data Management system (Phase 2): employee list with role-scoped access, employee creation with auto User account, tabbed profile with 5 tabs (Personal Info, Employment Details, Tax/BPJS, Documents, Emergency Contacts), document upload/download/delete, emergency contacts CRUD, employee deactivation, sidebar navigation, and seed data.</what-built>
  <how-to-verify>
1. Run `npx prisma db seed` to populate test data
2. Start dev server: `npm run dev`

**Test as HR Admin** (login with HR Admin seed account):
3. Verify sidebar shows "Karyawan" link
4. Click "Karyawan" -- see full employee list with all seeded employees
5. Test filters: filter by department, status (Active/Inactive), contract type
6. Test search: search by employee name or NIK
7. Click "Tambah Karyawan" -- fill minimum required fields, submit
8. Verify new employee appears in list and has auto-generated NIK
9. Click on an employee to view profile
10. Test each tab: edit a field in Personal Info, save -- verify toast and data persists on refresh
11. Test Employment Details tab: change department, save
12. Test Tax/BPJS tab: add NPWP, save
13. Test Documents tab: upload a PDF, verify it appears in list, download it, delete it
14. Test Emergency Contacts tab: add a contact, verify max 3 limit, edit a contact, delete a contact
15. Test Deactivation: click "Nonaktifkan Karyawan" on an active employee, fill date and reason, confirm. Verify employee status changes and they disappear from active list filter.

**Test as Manager** (login with Manager seed account):
16. Verify /employees shows only employees in manager's department
17. Click on an employee -- verify all tabs are read-only (no edit buttons, no upload, no add contact)
18. Try to navigate to an employee ID from another department via URL -- verify redirect

**Test as Employee** (login with Employee seed account):
19. Click "Karyawan" in sidebar -- verify redirect to own profile
20. Verify profile shows in read-only mode -- all fields visible but not editable
21. Verify documents and emergency contacts are visible but no add/upload/delete buttons

  </how-to-verify>
  <resume-signal>Type "approved" or describe any issues found</resume-signal>
</task>

</tasks>

<verification>
- `npm run build` passes without errors
- `npx prisma db seed` completes successfully
- All role-based access rules enforced (HR Admin: full CRUD, Manager: read-only department, Employee: read-only self)
- Employee creation atomically creates User account
- Employee deactivation deactivates both Employee and User
- Document upload/download/delete works end-to-end
- Emergency contacts CRUD works with max 3 limit
</verification>

<success_criteria>
1. HR Admin can create, view, edit, and deactivate employees
2. HR Admin can upload, download, and delete documents
3. HR Admin can add, edit, and delete emergency contacts (max 3)
4. Manager sees only department employees in read-only mode
5. Employee sees own profile in read-only mode
6. Deactivated employees cannot log in and do not appear in active lists
7. All 5 profile tabs are fully functional
8. Seed data provides comprehensive test coverage
9. Dashboard stubs link to employee section
</success_criteria>

<output>
After completion, create `.planning/phases/02-employee-data-management/02-08-SUMMARY.md`
</output>
