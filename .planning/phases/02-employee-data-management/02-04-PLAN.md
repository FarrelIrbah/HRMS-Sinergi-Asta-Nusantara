---
phase: 02-employee-data-management
plan: 04
type: execute
wave: 3
depends_on: ["02-02"]
files_modified:
  - src/app/(dashboard)/employees/page.tsx
  - src/app/(dashboard)/employees/_components/employee-columns.tsx
  - src/app/(dashboard)/employees/_components/employee-table.tsx
  - src/app/(dashboard)/employees/_components/employee-filters.tsx
  - src/components/layout/sidebar.tsx
autonomous: true

must_haves:
  truths:
    - "HR Admin sees a paginated, searchable, filterable list of all employees"
    - "Manager sees only employees in their own department (pre-filtered, no 403)"
    - "Employee role is redirected to their own profile page"
    - "Sidebar shows Karyawan link for all roles"
  artifacts:
    - path: "src/app/(dashboard)/employees/page.tsx"
      provides: "Employee list page with role-based data loading"
      min_lines: 30
    - path: "src/app/(dashboard)/employees/_components/employee-columns.tsx"
      provides: "DataTable column definitions"
      exports: ["columns"]
    - path: "src/app/(dashboard)/employees/_components/employee-table.tsx"
      provides: "DataTable wrapper"
    - path: "src/app/(dashboard)/employees/_components/employee-filters.tsx"
      provides: "Filter controls for department, status, contract type"
    - path: "src/components/layout/sidebar.tsx"
      provides: "Updated sidebar with Karyawan nav item"
      contains: "Karyawan"
  key_links:
    - from: "src/app/(dashboard)/employees/page.tsx"
      to: "src/lib/services/employee.service.ts"
      via: "server component calls getEmployees / getEmployeesForManager"
      pattern: "getEmployees|getEmployeesForManager"
    - from: "src/app/(dashboard)/employees/page.tsx"
      to: "auth"
      via: "role check for EMPLOYEE redirect"
      pattern: "redirect.*employees"
---

<objective>
Build the employee list page with DataTable, filters, role-based data scoping, sidebar navigation, and Employee role redirect.

Purpose: This is the main entry point for employee management. HR Admin sees all employees with full filters. Manager sees only their department's employees. Employee role gets redirected to their own profile. The sidebar needs a "Karyawan" link visible to all roles.
Output: Working /employees page with role-scoped data, search, filters, and navigation.
</objective>

<execution_context>
@C:\Users\farre\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\farre\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/02-employee-data-management/02-CONTEXT.md
@.planning/phases/02-employee-data-management/02-RESEARCH.md

@src/app/(dashboard)/master-data/page.tsx
@src/app/(dashboard)/users/page.tsx
@src/app/(dashboard)/users/_components/user-columns.tsx
@src/app/(dashboard)/users/_components/user-table.tsx
@src/components/shared/data-table.tsx
@src/components/layout/sidebar.tsx
@src/lib/services/employee.service.ts
@src/lib/constants.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create employee list page with role-based routing</name>
  <files>src/app/(dashboard)/employees/page.tsx, src/components/layout/sidebar.tsx</files>
  <action>
1. Update `src/components/layout/sidebar.tsx`:
   - Import `Users2` (or `UserRound`) from lucide-react
   - Add nav item after "Dashboard" entry:
     ```
     { label: "Karyawan", href: "/employees", icon: Users2, roles: ["SUPER_ADMIN", "HR_ADMIN", "MANAGER", "EMPLOYEE"] }
     ```
   - All roles see this link; page itself controls what they see

2. Create `src/app/(dashboard)/employees/page.tsx` as a server component:
   - Get session via auth()
   - If no session, redirect to /login
   - If role === "EMPLOYEE":
     - Look up their Employee record via getEmployeeByUserId(session.user.id)
     - If found, redirect(`/employees/${employee.id}`)
     - If not found, show "Profil karyawan tidak ditemukan" message
   - If role === "MANAGER":
     - Call getEmployeesForManager(session.user.id) with search/filter params from searchParams
   - If role === "HR_ADMIN" or "SUPER_ADMIN":
     - Call getEmployees() with search/filter params from searchParams
   - Parse searchParams for: page, search, departmentId, positionId, isActive, contractType
   - Fetch departments list (getAllDepartments) and positions list for filter dropdowns
   - Render page header "Karyawan" with "Tambah Karyawan" button (HR_ADMIN/SUPER_ADMIN only, links to /employees/new)
   - Render EmployeeFilters and EmployeeTable components
  </action>
  <verify>
Run `npx tsc --noEmit` -- no type errors.
Verify sidebar.tsx contains "Karyawan" nav item.
  </verify>
  <done>Employee list page loads with role-based data. EMPLOYEE redirects to own profile. MANAGER sees department-scoped list. HR_ADMIN sees all employees. Sidebar shows Karyawan link for all roles.</done>
</task>

<task type="auto">
  <name>Task 2: Create DataTable columns, table wrapper, and filters</name>
  <files>src/app/(dashboard)/employees/_components/employee-columns.tsx, src/app/(dashboard)/employees/_components/employee-table.tsx, src/app/(dashboard)/employees/_components/employee-filters.tsx</files>
  <action>
1. Create `employee-columns.tsx` following user-columns.tsx pattern:
   - Columns: Nama Lengkap, NIK, Departemen, Jabatan, Status (active badge), Tipe Kontrak, Tanggal Masuk, Aksi
   - Nama column: show namaLengkap with email below in muted text
   - Status column: green badge "Aktif" or red badge "Nonaktif" based on isActive
   - Tipe Kontrak: show "PKWT" or "PKWTT" badge
   - Tanggal Masuk: format with date-fns format(joinDate, "dd MMM yyyy")
   - Aksi column: dropdown menu with "Lihat Detail" (link to /employees/{id}) and "Edit" (link to /employees/{id}?tab=personal) -- Edit only shown for HR_ADMIN/SUPER_ADMIN
   - Mark this as "use client" component
   - Accept a `userRole` prop to conditionally show edit actions

2. Create `employee-table.tsx` following user-table.tsx pattern:
   - Accept employees data, pagination info, and userRole
   - Use the existing DataTable component from @/components/shared/data-table
   - Handle page navigation via URL search params (using nuqs or router.push)

3. Create `employee-filters.tsx`:
   - Department select dropdown (all departments from prop)
   - Position select dropdown (optionally filtered by selected department)
   - Employment status select: "Semua", "Aktif", "Nonaktif"
   - Contract type select: "Semua", "PKWT", "PKWTT"
   - Search input for name/NIK search
   - All filter changes update URL search params (use useRouter/useSearchParams or nuqs)
   - Follow existing filter patterns in the codebase (check audit-log-filters.tsx for reference)
   - "use client" component
  </action>
  <verify>
Run `npx tsc --noEmit` -- no type errors.
Verify all three component files exist and are importable.
  </verify>
  <done>Employee DataTable shows all required columns with proper formatting, badges, and action menus. Filters update URL params for server-side filtering. Table supports pagination.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes
- `/employees` page renders for HR_ADMIN with full employee list
- Manager sees pre-filtered department list (no 403 errors)
- Employee role redirects to their own profile
- Sidebar shows "Karyawan" for all roles
- Filters update URL and re-fetch filtered data
</verification>

<success_criteria>
1. Employee list shows columns: Name, NIK, Department, Position, Status, Contract Type, Join Date, Actions
2. Search by name or NIK works
3. Filters for department, position, status, and contract type work
4. Manager sees only their department's employees
5. Employee redirects to /employees/{their-id}
6. "Tambah Karyawan" button visible only to HR_ADMIN/SUPER_ADMIN
7. Sidebar updated with Karyawan link for all roles
</success_criteria>

<output>
After completion, create `.planning/phases/02-employee-data-management/02-04-SUMMARY.md`
</output>
