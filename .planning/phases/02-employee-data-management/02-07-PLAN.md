---
phase: 02-employee-data-management
plan: 07
type: execute
wave: 4
depends_on: ["02-03", "02-06"]
files_modified:
  - src/app/(dashboard)/employees/[id]/_components/documents-tab.tsx
  - src/app/(dashboard)/employees/[id]/_components/emergency-contacts-tab.tsx
  - src/app/(dashboard)/employees/[id]/_components/employee-profile-tabs.tsx
  - src/lib/actions/employee-document.actions.ts
autonomous: true

must_haves:
  truths:
    - "HR Admin can upload documents (PDF/JPG/PNG, max 5MB) to an employee profile"
    - "All roles can view and download documents on profiles they have access to"
    - "HR Admin can delete documents from an employee profile"
    - "HR Admin can add, edit, and delete emergency contacts (max 3 per employee)"
    - "Documents and emergency contacts tabs replace the placeholders from Plan 06"
  artifacts:
    - path: "src/app/(dashboard)/employees/[id]/_components/documents-tab.tsx"
      provides: "Document upload, list, download, delete UI"
      min_lines: 80
    - path: "src/app/(dashboard)/employees/[id]/_components/emergency-contacts-tab.tsx"
      provides: "Emergency contacts CRUD UI"
      min_lines: 80
    - path: "src/lib/actions/employee-document.actions.ts"
      provides: "Server actions for emergency contacts (documents use API routes)"
      exports: ["createEmergencyContactAction", "updateEmergencyContactAction", "deleteEmergencyContactAction"]
  key_links:
    - from: "src/app/(dashboard)/employees/[id]/_components/documents-tab.tsx"
      to: "/api/employees/[id]/documents"
      via: "fetch for upload/download/delete"
      pattern: "fetch.*api/employees"
    - from: "src/app/(dashboard)/employees/[id]/_components/emergency-contacts-tab.tsx"
      to: "src/lib/actions/employee-document.actions.ts"
      via: "form calls emergency contact actions"
      pattern: "createEmergencyContactAction|deleteEmergencyContactAction"
---

<objective>
Build the Documents tab and Emergency Contacts tab for the employee profile, replacing the placeholders from Plan 06.

Purpose: Documents tab (EMP-04) provides upload/view/download/delete for employee documents using the API routes from Plan 03. Emergency Contacts tab (EMP-01) provides CRUD for up to 3 emergency contacts per employee. Both tabs respect the same read-only mode for Manager/Employee roles.
Output: Fully functional Documents and Emergency Contacts tabs integrated into the employee profile.
</objective>

<execution_context>
@C:\Users\farre\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\farre\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/02-employee-data-management/02-CONTEXT.md
@.planning/phases/02-employee-data-management/02-RESEARCH.md

@src/app/(dashboard)/employees/[id]/_components/employee-profile-tabs.tsx
@src/app/api/employees/[id]/documents/route.ts
@src/app/api/employees/[id]/documents/[docId]/route.ts
@src/lib/services/employee-document.service.ts
@src/lib/services/employee.service.ts
@src/lib/validations/employee.ts
@src/lib/constants.ts
@src/components/shared/confirm-dialog.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build Documents tab with upload/download/delete</name>
  <files>src/app/(dashboard)/employees/[id]/_components/documents-tab.tsx</files>
  <action>
Create `src/app/(dashboard)/employees/[id]/_components/documents-tab.tsx` ("use client"):

Props: { employeeId: string, documents: EmployeeDocument[], readOnly: boolean }

**Document List:**
- Display existing documents in a table or card grid
- Columns: Tipe Dokumen (using DOCUMENT_TYPE_LABELS), Nama File, Ukuran (format bytes to KB/MB), Tanggal Upload (formatted with date-fns), Aksi
- Each document row has:
  - Download button: triggers fetch to GET /api/employees/{employeeId}/documents/{docId}, then creates a blob URL and triggers browser download
  - Delete button (HR_ADMIN only, hidden in readOnly mode): shows ConfirmDialog, then calls DELETE /api/employees/{employeeId}/documents/{docId}
- Empty state: "Belum ada dokumen yang diunggah"

**Upload Section (hidden in readOnly mode):**
- Document type select dropdown (DocumentType enum values using DOCUMENT_TYPE_LABELS)
- File input (accept=".pdf,.jpg,.jpeg,.png")
- Upload button "Unggah Dokumen"
- On upload:
  1. Validate file selected and type selected
  2. Create FormData with file and documentType
  3. POST to /api/employees/{employeeId}/documents
  4. Show loading state on button
  5. On success: toast("Dokumen berhasil diunggah"), router.refresh() to reload documents
  6. On error: toast.error with error message from response
  7. Reset file input after success

**File size display helper:**
```typescript
function formatFileSize(bytes: number): string {
  if (bytes < 1024) return `${bytes} B`;
  if (bytes < 1024 * 1024) return `${(bytes / 1024).toFixed(1)} KB`;
  return `${(bytes / (1024 * 1024)).toFixed(1)} MB`;
}
```

Use lucide-react icons: Upload, Download, Trash2, FileText, Image.
  </action>
  <verify>
Run `npx tsc --noEmit` -- no type errors.
  </verify>
  <done>Documents tab shows document list with download/delete actions, upload form with file type/size validation, and respects read-only mode.</done>
</task>

<task type="auto">
  <name>Task 2: Build Emergency Contacts tab and server actions, update profile tabs</name>
  <files>src/lib/actions/employee-document.actions.ts, src/app/(dashboard)/employees/[id]/_components/emergency-contacts-tab.tsx, src/app/(dashboard)/employees/[id]/_components/employee-profile-tabs.tsx</files>
  <action>
1. Create `src/lib/actions/employee-document.actions.ts` ("use server"):
   This file handles emergency contact server actions (naming follows the pattern of grouping employee sub-resources).

   Functions:
   - `createEmergencyContactAction(employeeId, formData)`:
     - Auth check: HR_ADMIN/SUPER_ADMIN only
     - Validate with emergencyContactSchema
     - Check count: if employee already has 3 contacts, return error "Maksimal 3 kontak darurat"
     - Create EmergencyContact record via prisma
     - Audit log: CREATE, MODULES.EMERGENCY_CONTACT
     - revalidatePath(`/employees/${employeeId}`)

   - `updateEmergencyContactAction(contactId, formData)`:
     - Auth check: HR_ADMIN/SUPER_ADMIN only
     - Validate with emergencyContactSchema
     - Update EmergencyContact record
     - Audit log: UPDATE, MODULES.EMERGENCY_CONTACT
     - revalidatePath for the employee

   - `deleteEmergencyContactAction(contactId)`:
     - Auth check: HR_ADMIN/SUPER_ADMIN only
     - Delete EmergencyContact record
     - Audit log: DELETE, MODULES.EMERGENCY_CONTACT
     - revalidatePath

2. Create `src/app/(dashboard)/employees/[id]/_components/emergency-contacts-tab.tsx` ("use client"):

   Props: { employeeId: string, contacts: EmergencyContact[], readOnly: boolean }

   **Contact List:**
   - Display contacts in cards (not table -- up to 3, cards look better)
   - Each card shows: Nama, Hubungan (relationship), Nomor Telepon, Alamat (if present)
   - Each card has Edit and Delete buttons (hidden in readOnly mode)
   - Delete uses ConfirmDialog

   **Add Contact (hidden in readOnly mode):**
   - "Tambah Kontak Darurat" button (hidden when 3 contacts already exist)
   - On click: shows inline form or dialog with fields: Nama (required), Hubungan (required -- suggest common values: Suami, Istri, Orang Tua, Saudara, Anak), Nomor Telepon (required), Alamat (optional)
   - Use react-hook-form with emergencyContactSchema
   - On submit: call createEmergencyContactAction
   - On success: toast + router.refresh()

   **Edit Contact:**
   - On edit click: show same form pre-populated with contact data
   - On submit: call updateEmergencyContactAction
   - On success: toast + router.refresh()

   Empty state: "Belum ada kontak darurat"

3. Update `employee-profile-tabs.tsx`:
   - Replace the two placeholder divs with actual DocumentsTab and EmergencyContactsTab components
   - Pass employee.documents and employee.emergencyContacts to the respective tabs
   - Pass employeeId and readOnly to both tabs
  </action>
  <verify>
Run `npx tsc --noEmit` -- no type errors.
Verify employee-profile-tabs.tsx no longer has placeholder text.
  </verify>
  <done>Emergency contacts tab supports add/edit/delete with max 3 limit. Documents tab is wired in. Profile tabs component renders all 5 tabs with real components. All tabs respect read-only mode.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes
- Documents tab: upload works, download returns correct file, delete removes record and file
- Emergency contacts: can add up to 3, edit existing, delete existing
- Both tabs respect readOnly mode (no upload/add/edit/delete buttons for Manager/Employee)
- Profile tabs component has no placeholder divs remaining
</verification>

<success_criteria>
1. Documents tab shows list of uploaded documents with type, name, size, date
2. Upload accepts only PDF/JPG/PNG up to 5MB with type selection
3. Download streams file with correct filename
4. Delete removes both DB record and file from disk
5. Emergency contacts show in cards with name, relationship, phone, address
6. Max 3 contacts enforced (add button hidden at limit)
7. Add/edit uses form with validation
8. All 5 profile tabs are now fully functional
9. Read-only mode hides all mutation controls
</success_criteria>

<output>
After completion, create `.planning/phases/02-employee-data-management/02-07-SUMMARY.md`
</output>
