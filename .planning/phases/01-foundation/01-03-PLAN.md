---
phase: 01-foundation
plan: 03
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - src/app/(dashboard)/layout.tsx
  - src/app/(dashboard)/dashboard/page.tsx
  - src/components/layout/sidebar.tsx
  - src/components/layout/header.tsx
  - src/components/layout/breadcrumbs.tsx
  - src/components/shared/stat-card.tsx
  - src/components/shared/loading-skeleton.tsx
  - src/components/shared/confirm-dialog.tsx
autonomous: true

must_haves:
  truths:
    - "Authenticated users see a sidebar navigation and header when on dashboard routes"
    - "Sidebar shows navigation links appropriate to the user's role"
    - "User can log out from the header on any dashboard page (AUTH-04)"
    - "Dashboard page renders without errors for authenticated users"
    - "Layout is responsive (sidebar collapses on mobile)"
  artifacts:
    - path: "src/app/(dashboard)/layout.tsx"
      provides: "Protected dashboard layout with sidebar and header"
      contains: "auth"
    - path: "src/components/layout/sidebar.tsx"
      provides: "Navigation sidebar with role-based menu items"
      contains: "sidebar"
    - path: "src/components/layout/header.tsx"
      provides: "Top header with user info and logout button"
      contains: "signOut"
    - path: "src/components/shared/stat-card.tsx"
      provides: "Reusable stat card component for dashboard widgets"
      contains: "StatCard"
    - path: "src/components/shared/confirm-dialog.tsx"
      provides: "Reusable confirmation dialog for destructive actions"
      contains: "ConfirmDialog"
  key_links:
    - from: "src/app/(dashboard)/layout.tsx"
      to: "src/lib/auth.ts"
      via: "auth() session check"
      pattern: "auth\\(\\)"
    - from: "src/components/layout/header.tsx"
      to: "next-auth/react"
      via: "signOut function"
      pattern: "signOut"
    - from: "src/components/layout/sidebar.tsx"
      to: "src/lib/constants.ts"
      via: "Role-based navigation items"
      pattern: "ROLES|role"
---

<objective>
Build the dashboard layout shell: sidebar navigation, header with user info and logout, breadcrumbs, and reusable shared components (stat card, loading skeleton, confirm dialog).

Purpose: Every dashboard feature (user management, master data, audit log, dashboard widgets) renders inside this layout. This is the application shell that gives the system its shape.

Output: A fully functional dashboard layout with role-based sidebar navigation, logout button, and reusable UI components.
</objective>

<execution_context>
@C:\Users\farre\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\farre\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/01-foundation/01-RESEARCH.md
@.planning/phases/01-foundation/01-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create dashboard layout with sidebar and header</name>
  <files>
    src/app/(dashboard)/layout.tsx
    src/components/layout/sidebar.tsx
    src/components/layout/header.tsx
    src/components/layout/breadcrumbs.tsx
  </files>
  <action>
    1. Create `src/components/layout/sidebar.tsx` -- a client component ('use client'):
    - Uses `useSession()` to get the current user's role
    - Navigation items defined as an array, filtered by role:
      - "Dashboard" (/dashboard) -- icon: LayoutDashboard -- ALL roles
      - "Pengguna" (/users) -- icon: Users -- SUPER_ADMIN only
      - "Data Master" (/master-data) -- icon: Database -- SUPER_ADMIN only
      - "Log Audit" (/audit-log) -- icon: FileText -- SUPER_ADMIN only
    - Each nav item: icon + label, active state (highlight current route using `usePathname()`)
    - Mobile: Sheet component (slide-out drawer) triggered by hamburger button
    - Desktop: Fixed sidebar (w-64) with company name "HRMS PT. SAN" at the top
    - Use shadcn/ui Sheet, Button, Separator components
    - Tailwind styling: dark sidebar background (slate-900 or similar), white text, hover highlights
    - Company logo area at top (text "HRMS" + "PT. Sinergi Asta Nusantara" below it)

    2. Create `src/components/layout/header.tsx` -- a client component ('use client'):
    - Uses `useSession()` to display current user's name and role
    - Right side: User avatar (initials from name), dropdown menu with:
      - User name and email (display only)
      - Role badge (Indonesian label from constants)
      - Separator
      - "Keluar" (Logout) button -- calls `signOut({ callbackUrl: "/login" })`
    - Left side: Mobile hamburger menu trigger (visible only on small screens)
    - Use shadcn/ui Avatar, DropdownMenu, Badge, Button

    3. Create `src/components/layout/breadcrumbs.tsx` -- a client component:
    - Uses `usePathname()` to generate breadcrumb trail
    - Maps path segments to Indonesian labels: dashboard -> "Dashboard", users -> "Pengguna", master-data -> "Data Master", audit-log -> "Log Audit"
    - Renders as: Home > Current Page (using shadcn Separator or custom divider)
    - Simple and functional, no over-engineering

    4. Create `src/app/(dashboard)/layout.tsx` -- a SERVER component:
    - Calls `auth()` from `@/lib/auth` to get the session
    - If no session, `redirect("/login")`
    - Layout structure:
      ```
      <div className="flex h-screen">
        <Sidebar />
        <div className="flex-1 flex flex-col overflow-hidden">
          <Header />
          <main className="flex-1 overflow-y-auto p-6">
            <Breadcrumbs />
            {children}
          </main>
        </div>
      </div>
      ```
    - Pass session data to client components via props or rely on useSession()

    5. Create `src/app/(dashboard)/dashboard/page.tsx` -- a placeholder:
    - Server component that displays "Dashboard" heading
    - Simple text: "Selamat datang di HRMS PT. Sinergi Asta Nusantara"
    - This will be replaced with role-specific widgets in Plan 09

    ALL UI labels must be in Indonesian (Bahasa Indonesia).
  </action>
  <verify>
    - After login, user sees sidebar + header + main content area
    - Sidebar shows role-appropriate navigation items (Super Admin sees all 4, Employee sees only Dashboard)
    - Logout button in header works (redirects to /login and clears session)
    - Mobile: sidebar collapses into a hamburger menu
    - Breadcrumbs show correct path labels in Indonesian
  </verify>
  <done>
    Dashboard layout shell renders with sidebar, header, breadcrumbs. Navigation is role-filtered. Logout works via header dropdown (AUTH-04 complete). Responsive on mobile.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create reusable shared components</name>
  <files>
    src/components/shared/stat-card.tsx
    src/components/shared/loading-skeleton.tsx
    src/components/shared/confirm-dialog.tsx
    src/components/shared/data-table.tsx
    src/components/shared/data-table-pagination.tsx
  </files>
  <action>
    1. Create `src/components/shared/stat-card.tsx`:
    - Props: title (string), value (string | number), description (string optional), icon (LucideIcon), trend (optional: { value: number, isPositive: boolean })
    - Uses shadcn Card component
    - Layout: icon top-left, title below icon, large value, optional description/trend
    - Example: StatCard title="Total Karyawan" value={0} icon={Users}
    - This component will be used for dashboard summary widgets

    2. Create `src/components/shared/loading-skeleton.tsx`:
    - A generic loading skeleton component for tables and cards
    - Export `TableSkeleton` -- renders N rows of skeleton placeholders (configurable via `rows` prop, default 5)
    - Export `CardSkeleton` -- renders a card-shaped skeleton
    - Export `StatCardSkeleton` -- matches StatCard dimensions
    - Uses shadcn Skeleton component

    3. Create `src/components/shared/confirm-dialog.tsx`:
    - Reusable confirmation dialog for destructive actions (delete, deactivate)
    - Props: open (boolean), onOpenChange, title (string), description (string), onConfirm (function), confirmText (string, default "Konfirmasi"), cancelText (string, default "Batal"), variant (default | destructive)
    - Uses shadcn AlertDialog components
    - Destructive variant: red confirm button
    - All default text in Indonesian

    4. Create `src/components/shared/data-table.tsx`:
    - Reusable data table component using @tanstack/react-table
    - Props: columns (ColumnDef[]), data (T[]), searchKey (string optional for search input), searchPlaceholder (string optional)
    - Renders shadcn Table component
    - Includes optional search input above the table (filters on searchKey column)
    - Empty state: "Tidak ada data" (No data) when table is empty
    - This is the BASE table -- individual pages will define their own columns

    5. Create `src/components/shared/data-table-pagination.tsx`:
    - Pagination component that works with the data-table
    - Props: table (TanStack Table instance), pageSizeOptions (number[], default [25, 50])
    - Shows: "Menampilkan X dari Y data" (Showing X of Y data)
    - Page size selector: "Baris per halaman" (Rows per page)
    - Previous/Next buttons with disabled states
    - Page number display
    - All labels in Indonesian
  </action>
  <verify>
    - All components export correctly (no TypeScript errors)
    - `npx tsc --noEmit` passes
    - Components can be imported in a test page without errors
  </verify>
  <done>
    Reusable shared components created: StatCard, LoadingSkeleton variants, ConfirmDialog, DataTable with pagination. All labeled in Indonesian. Ready for use in subsequent plans.
  </done>
</task>

</tasks>

<verification>
- Login -> see dashboard layout with sidebar and header
- Sidebar navigation items filter by role
- Logout button in header dropdown works
- Mobile responsive: sidebar becomes hamburger menu
- All shared components export without type errors
- All UI labels in Indonesian
</verification>

<success_criteria>
- Dashboard layout shell is complete with sidebar, header, breadcrumbs
- AUTH-04 (logout) fully implemented via header dropdown
- Role-based navigation filtering works
- Responsive design for mobile
- 5 reusable shared components ready (StatCard, LoadingSkeleton, ConfirmDialog, DataTable, DataTablePagination)
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-03-SUMMARY.md`
</output>
