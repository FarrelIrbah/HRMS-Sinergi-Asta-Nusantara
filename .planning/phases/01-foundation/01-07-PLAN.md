---
phase: 01-foundation
plan: 07
type: execute
wave: 3
depends_on: ["01-03", "01-04"]
files_modified:
  - src/lib/services/master-data.service.ts
  - src/lib/actions/master-data.actions.ts
  - src/app/(dashboard)/master-data/_components/office-location-tab.tsx
  - src/app/(dashboard)/master-data/_components/office-location-form-dialog.tsx
  - src/app/(dashboard)/master-data/_components/leave-type-tab.tsx
  - src/app/(dashboard)/master-data/_components/leave-type-form-dialog.tsx
  - src/app/(dashboard)/master-data/_components/master-data-tabs.tsx
autonomous: true

must_haves:
  truths:
    - "Super Admin can create, edit, and soft-delete office locations with IP and/or GPS config (MASTER-03)"
    - "Super Admin can create, edit, and soft-delete leave types with quota and optional gender restriction (MASTER-04)"
    - "Office location form has separate sections for IP range and GPS + radius"
    - "Leave type quota is a single number (days per year)"
    - "All master data operations are recorded in the audit log (AUDIT-01)"
  artifacts:
    - path: "src/app/(dashboard)/master-data/_components/office-location-tab.tsx"
      provides: "Office locations tab with data table and CRUD"
      contains: "OfficeLocationTab"
    - path: "src/app/(dashboard)/master-data/_components/leave-type-tab.tsx"
      provides: "Leave types tab with data table and CRUD"
      contains: "LeaveTypeTab"
  key_links:
    - from: "src/lib/actions/master-data.actions.ts"
      to: "src/lib/services/master-data.service.ts"
      via: "Server Actions for office location and leave type mutations"
      pattern: "createOfficeLocation|createLeaveType"
    - from: "src/app/(dashboard)/master-data/_components/master-data-tabs.tsx"
      to: "office-location-tab.tsx and leave-type-tab.tsx"
      via: "Tab rendering"
      pattern: "OfficeLocationTab|LeaveTypeTab"
---

<objective>
Implement office location and leave type CRUD -- the remaining two tabs on the master data page. Office locations support IP range and GPS+radius configuration. Leave types support annual quota with optional gender restriction.

Purpose: MASTER-03 establishes attendance restriction zones (needed by Phase 3 attendance clock-in). MASTER-04 establishes leave type definitions (needed by Phase 3 leave management). Both are foundational master data that later phases depend on.

Output: Complete master data page with all 4 tabs functional: Departemen, Jabatan, Lokasi Kantor, Jenis Cuti.
</objective>

<execution_context>
@C:\Users\farre\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\farre\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/01-foundation/01-RESEARCH.md
@.planning/phases/01-foundation/01-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add office location and leave type service functions and actions</name>
  <files>
    src/lib/services/master-data.service.ts
    src/lib/actions/master-data.actions.ts
  </files>
  <action>
    NOTE: Plan 06 may have already created these files. If so, ADD to the existing files (do not overwrite). If Plan 06 has not run yet or files don't exist, create them. Check file existence first and append the new functions.

    1. Add to `src/lib/services/master-data.service.ts` -- Office Location functions:
    - `getOfficeLocations(params)`: Paginated list with search by name. Filter `deletedAt: null`. Returns PaginatedResponse.
    - `getAllOfficeLocations()`: Non-paginated list for dropdowns. Filter `deletedAt: null`. Returns `{ id, name }[]`.
    - `createOfficeLocation(data, actorId)`: Create with all fields (name, address, allowedIPs array, latitude, longitude, radiusMeters). Call createAuditLog.
    - `updateOfficeLocation(id, data, actorId)`: Fetch old, update, audit log.
    - `deleteOfficeLocation(id, actorId)`: SOFT DELETE. Call createAuditLog. (In Phase 3, check for assigned employees before allowing delete.)

    Leave Type functions:
    - `getLeaveTypes(params)`: Paginated list with search by name. Filter `deletedAt: null`. Returns PaginatedResponse.
    - `getAllLeaveTypes()`: Non-paginated list for dropdowns. Filter `deletedAt: null`. Returns `{ id, name, annualQuota }[]`.
    - `createLeaveType(data, actorId)`: Create with name, annualQuota, isPaid, genderRestriction. Call createAuditLog.
    - `updateLeaveType(id, data, actorId)`: Fetch old, update, audit log.
    - `deleteLeaveType(id, actorId)`: SOFT DELETE. Call createAuditLog.

    2. Add to `src/lib/actions/master-data.actions.ts` -- Server Actions:
    - Office location actions: `createOfficeLocationAction`, `updateOfficeLocationAction`, `deleteOfficeLocationAction`
    - Leave type actions: `createLeaveTypeAction`, `updateLeaveTypeAction`, `deleteLeaveTypeAction`
    - Same pattern: check session, verify SUPER_ADMIN, validate with Zod (schemas from validations/master-data.ts), call service, revalidate "/master-data".
  </action>
  <verify>
    - `npx tsc --noEmit` passes
    - All new service functions filter by `deletedAt: null`
    - All mutations call createAuditLog
    - No conflicts with existing department/position functions in the same files
  </verify>
  <done>
    Service functions and server actions for office locations and leave types. Soft delete, audit logging, validation all in place.
  </done>
</task>

<task type="auto">
  <name>Task 2: Build office location tab UI</name>
  <files>
    src/app/(dashboard)/master-data/_components/office-location-tab.tsx
    src/app/(dashboard)/master-data/_components/office-location-form-dialog.tsx
  </files>
  <action>
    1. Create `src/app/(dashboard)/master-data/_components/office-location-tab.tsx` -- Client Component:
    - Data table with columns: Nama (name), Alamat (address), IP Range (show count or "Tidak dikonfigurasi"), GPS (show "Lat, Long (Rm)" or "Tidak dikonfigurasi"), Tanggal Dibuat, Aksi
    - "Tambah Lokasi" button above table
    - Actions: "Edit", "Hapus" (with ConfirmDialog)
    - Search: "Cari lokasi kantor..."

    2. Create `src/app/(dashboard)/master-data/_components/office-location-form-dialog.tsx` -- Client Component:
    - Dialog for create/edit office location
    - Form with TWO SECTIONS as specified in CONTEXT.md:

    Section 1 - "Rentang IP" (IP Range):
    - Dynamic list of IP inputs (add/remove)
    - Each entry: text input for IP range (e.g., "192.168.1.0/24" or "10.0.0.1-10.0.0.255")
    - "Tambah IP" button to add more entries
    - Optional: admin can leave this section empty

    Section 2 - "Koordinat GPS" (GPS Coordinates):
    - Latitude: number input (label "Latitude")
    - Longitude: number input (label "Longitude")
    - Radius: number input in meters (label "Radius (meter)", min 50, max 10000)
    - Optional: admin can leave this section empty

    Other fields:
    - Nama (name): text input, required
    - Alamat (address): textarea, optional

    NOTE from CONTEXT.md: "admin can fill either or both" sections. At least one section (IP or GPS) should ideally be filled, but do NOT enforce this at form level -- leave it as a business decision.

    - Submit: "Simpan", toast on success/error
  </action>
  <verify>
    - Office locations tab shows data table
    - Create: dialog with two sections (IP and GPS), both optional
    - Can add/remove IP ranges dynamically
    - GPS fields accept decimal coordinates
    - Edit: dialog pre-filled with existing data
    - Delete: soft delete with confirm dialog
    - Audit log entries created
  </verify>
  <done>
    Office locations tab with two-section form (IP range + GPS). CRUD with soft delete. MASTER-03 complete.
  </done>
</task>

<task type="auto">
  <name>Task 3: Build leave type tab UI and wire remaining tabs</name>
  <files>
    src/app/(dashboard)/master-data/_components/leave-type-tab.tsx
    src/app/(dashboard)/master-data/_components/leave-type-form-dialog.tsx
    src/app/(dashboard)/master-data/_components/master-data-tabs.tsx
  </files>
  <action>
    1. Create `src/app/(dashboard)/master-data/_components/leave-type-tab.tsx` -- Client Component:
    - Data table with columns: Nama (name), Kuota Tahunan (annual quota, show as "X hari"), Berbayar (isPaid, badge "Ya"/"Tidak"), Pembatasan Gender (genderRestriction, show "Semua"/"Pria"/"Wanita"), Tanggal Dibuat, Aksi
    - "Tambah Jenis Cuti" button
    - Actions: "Edit", "Hapus" (with ConfirmDialog)
    - Search: "Cari jenis cuti..."

    2. Create `src/app/(dashboard)/master-data/_components/leave-type-form-dialog.tsx` -- Client Component:
    - Dialog for create/edit leave type
    - Fields:
      - Nama (name): text input (e.g., "Cuti Tahunan", "Cuti Sakit", "Cuti Melahirkan")
      - Kuota Tahunan (annual quota): number input, "hari" suffix label, min 0
      - Berbayar (isPaid): checkbox or toggle switch, default checked
      - Pembatasan Gender (genderRestriction): Select with options "Semua" (null/none), "Pria" (MALE), "Wanita" (FEMALE). Default "Semua".
    - Submit: "Simpan", toast on success/error

    3. Update `src/app/(dashboard)/master-data/_components/master-data-tabs.tsx`:
    - Replace the placeholder content for "Lokasi Kantor" tab with OfficeLocationTab component
    - Replace the placeholder content for "Jenis Cuti" tab with LeaveTypeTab component
    - All 4 tabs now render their respective functional components

    All labels in Indonesian.
  </action>
  <verify>
    - Leave types tab shows data table with quota and gender columns
    - Create leave type: dialog with name, quota, paid toggle, gender select
    - Edit/delete work correctly with soft delete
    - All 4 tabs on master data page are now functional (no more placeholders)
    - Tab switching preserves selection via URL param
    - Audit log entries created for all operations
  </verify>
  <done>
    Leave types tab fully functional. All 4 master data tabs operational. MASTER-04 complete. Complete master data management page.
  </done>
</task>

</tasks>

<verification>
- Office locations: create with IP ranges and/or GPS coordinates, edit, soft delete (MASTER-03)
- Leave types: create with quota and gender restriction, edit, soft delete (MASTER-04)
- All 4 tabs functional on /master-data page
- Soft-deleted records hidden from all lists
- All operations audit-logged (AUDIT-01)
- All UI labels in Indonesian
</verification>

<success_criteria>
- MASTER-03: Office location CRUD with IP + GPS config -- COMPLETE
- MASTER-04: Leave type CRUD with quota and gender restriction -- COMPLETE
- AUDIT-01: All operations logged -- COMPLETE
- Master data page fully operational with 4 tabs
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-07-SUMMARY.md`
</output>
