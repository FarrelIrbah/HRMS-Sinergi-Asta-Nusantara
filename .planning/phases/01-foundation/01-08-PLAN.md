---
phase: 01-foundation
plan: 08
type: execute
wave: 4
depends_on: ["01-04", "01-05", "01-06", "01-07"]
files_modified:
  - src/app/(dashboard)/audit-log/page.tsx
  - src/app/(dashboard)/audit-log/_components/audit-log-table.tsx
  - src/app/(dashboard)/audit-log/_components/audit-log-columns.tsx
  - src/app/(dashboard)/audit-log/_components/audit-log-filters.tsx
  - src/app/(dashboard)/audit-log/[id]/page.tsx
autonomous: true

must_haves:
  truths:
    - "Super Admin can view the audit log showing all system actions (AUDIT-02)"
    - "Super Admin can filter audit log by user, date range, and module (AUDIT-02)"
    - "Audit log shows timestamp, user, action, module, target, and old/new values"
    - "Clicking an audit log entry shows full before/after diff detail view"
    - "Filters are always visible in a bar above the table (CONTEXT.md)"
    - "Audit log is paginated at 25 or 50 rows per page (CONTEXT.md)"
  artifacts:
    - path: "src/app/(dashboard)/audit-log/page.tsx"
      provides: "Audit log list page"
      contains: "AuditLogPage"
    - path: "src/app/(dashboard)/audit-log/[id]/page.tsx"
      provides: "Audit log detail page showing full before/after diff"
      contains: "AuditLogDetailPage"
    - path: "src/app/(dashboard)/audit-log/_components/audit-log-filters.tsx"
      provides: "Filter bar for audit log"
      contains: "AuditLogFilters"
  key_links:
    - from: "src/app/(dashboard)/audit-log/page.tsx"
      to: "src/lib/services/audit.service.ts"
      via: "Server component fetching audit log data"
      pattern: "getAuditLogs"
    - from: "src/app/(dashboard)/audit-log/_components/audit-log-filters.tsx"
      to: "nuqs"
      via: "URL search params for persistent filters"
      pattern: "useQueryState|parseAsString"
---

<objective>
Build the audit log viewer page with filterable table and detail view for viewing full before/after diffs.

Purpose: AUDIT-02 requires Super Admin to view and filter the audit log. This is the visibility layer for the audit infrastructure built in Plan 04 and populated by Plans 05-07.

Output: /audit-log page with filter bar (user, date range, module), paginated table, and /audit-log/[id] detail page showing full before/after values.
</objective>

<execution_context>
@C:\Users\farre\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\farre\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/01-foundation/01-RESEARCH.md
@.planning/phases/01-foundation/01-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build audit log list page with filters</name>
  <files>
    src/app/(dashboard)/audit-log/page.tsx
    src/app/(dashboard)/audit-log/_components/audit-log-table.tsx
    src/app/(dashboard)/audit-log/_components/audit-log-columns.tsx
    src/app/(dashboard)/audit-log/_components/audit-log-filters.tsx
  </files>
  <action>
    1. Create `src/app/(dashboard)/audit-log/_components/audit-log-filters.tsx` -- Client Component:
    - A PERSISTENT filter bar above the table (CONTEXT.md: "Filters always visible in a bar above the table")
    - Filter controls in a horizontal row:
      - Pengguna (User): Select dropdown populated from `getAuditLogUsers()`. Label "Semua Pengguna" for empty/all.
      - Modul (Module): Select dropdown populated from `getAuditLogModules()`. Label "Semua Modul" for empty/all.
      - Tanggal Dari (Date From): Date input
      - Tanggal Sampai (Date To): Date input
      - "Terapkan" (Apply) button to apply filters
      - "Reset" button to clear all filters
    - Use `nuqs` for URL-based filter state so filters persist across navigation and page refresh
    - Filters are synced to URL search params: ?userId=xxx&module=xxx&dateFrom=xxx&dateTo=xxx

    2. Create `src/app/(dashboard)/audit-log/_components/audit-log-columns.tsx`:
    - Column definitions for the audit log table:
      - Waktu (Timestamp): formatted with date-fns Indonesian locale, format "dd MMM yyyy, HH:mm:ss"
      - Pengguna (User): user name
      - Aksi (Action): badge with Indonesian label (Buat/Ubah/Hapus) and color coding (CREATE: green, UPDATE: blue, DELETE: red)
      - Modul (Module): module name (already in Indonesian from constants)
      - Target ID: truncated to first 8 chars
      - Nilai Lama/Baru (Old/New Value): Show "Lihat Detail" link if values exist, "-" if null
    - "Lihat Detail" links to /audit-log/[id]

    3. Create `src/app/(dashboard)/audit-log/_components/audit-log-table.tsx` -- Client Component:
    - Uses shared DataTable component with audit log columns
    - Shows total count: "Total: X entri" (Total: X entries)
    - Pagination using shared DataTablePagination (25 or 50 rows per page as per CONTEXT.md)

    4. Create `src/app/(dashboard)/audit-log/page.tsx` -- Server Component:
    - Check session, verify SUPER_ADMIN. If not, redirect to /dashboard.
    - Parse search params (userId, module, dateFrom, dateTo, page, pageSize)
    - Fetch audit logs via `getAuditLogs` with parsed filters
    - Fetch filter options: users list and module list
    - Page title: "Log Audit" (Audit Log)
    - Render: AuditLogFilters (with filter options), then AuditLogTable (with data)

    CONTEXT.md spec: "Table columns: Timestamp, User, Action (create/update/delete), Module, Target, Old Value, New Value"
    CONTEXT.md spec: "Paginated table -- 25 or 50 rows per page"
    CONTEXT.md spec: "Filters always visible in a bar above the table (user, date range, module)"
  </action>
  <verify>
    - /audit-log page loads for Super Admin
    - Filter bar is visible above the table
    - User dropdown shows users who have audit entries
    - Module dropdown shows modules with audit entries
    - Date range filters work
    - Table shows audit log entries from Plans 05-07 operations
    - Pagination works with 25/50 page sizes
    - Filter state persists in URL
  </verify>
  <done>
    Audit log list page with persistent filter bar, paginated table, URL-based filter state. All columns formatted with Indonesian labels.
  </done>
</task>

<task type="auto">
  <name>Task 2: Build audit log detail page with before/after diff</name>
  <files>
    src/app/(dashboard)/audit-log/[id]/page.tsx
  </files>
  <action>
    1. Create `src/app/(dashboard)/audit-log/[id]/page.tsx` -- Server Component:
    - Check session, verify SUPER_ADMIN
    - Fetch audit log entry by ID via `getAuditLogById`
    - If not found, show "Entri tidak ditemukan" (Entry not found) or redirect
    - Page title: "Detail Log Audit" (Audit Log Detail)

    Display layout:
    - Header section with:
      - Aksi (Action): badge with color
      - Modul (Module)
      - Pengguna (User): name and email
      - Waktu (Timestamp): full date-time format
      - Target ID

    - Before/After diff section using two-column layout:
      - Left column: "Nilai Sebelum" (Old Value) -- formatted JSON
      - Right column: "Nilai Sesudah" (New Value) -- formatted JSON
      - For CREATE: Old Value shows "-" (nothing before), New Value shows the created data
      - For DELETE: Old Value shows the deleted data, New Value shows "-"
      - For UPDATE: Both columns show the data, with changed fields visually highlighted (different background color for changed values)

    - JSON formatting: Pretty-print JSON with indentation. For each key-value pair, show the key in bold and the value as text. Highlight differences between old and new values (changed keys have a colored background).

    - "Kembali" (Back) button that navigates back to /audit-log (preserving filter params if possible)

    CONTEXT.md spec: "Old Value / New Value for complex records: link to a detail view showing the full before/after diff"
  </action>
  <verify>
    - Clicking "Lihat Detail" from audit log table navigates to /audit-log/[id]
    - Detail page shows all entry metadata (action, module, user, timestamp)
    - Before/after values displayed in two-column layout
    - CREATE entries show empty old value, populated new value
    - UPDATE entries show both with differences highlighted
    - DELETE entries show populated old value, empty new value
    - Back button returns to audit log list
  </verify>
  <done>
    Audit log detail page with full before/after diff view. AUDIT-02 fully complete with both list and detail views.
  </done>
</task>

</tasks>

<verification>
- AUDIT-02: Super Admin can view audit log with filters by user, date range, and module
- Filter bar always visible above table
- Pagination at 25/50 rows
- Detail view shows full before/after diff
- All labels in Indonesian
- Non-Super Admin cannot access audit log
</verification>

<success_criteria>
- AUDIT-02: Audit log viewer with filters -- COMPLETE
- List view: paginated table with all required columns
- Detail view: full before/after diff for complex records
- Filters: user, date range, module -- always visible, URL-persisted
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-08-SUMMARY.md`
</output>
