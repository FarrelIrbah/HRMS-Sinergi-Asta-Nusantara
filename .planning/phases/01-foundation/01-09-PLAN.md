---
phase: 01-foundation
plan: 09
type: execute
wave: 4
depends_on: ["01-05", "01-06", "01-07"]
files_modified:
  - src/app/(dashboard)/dashboard/page.tsx
  - src/app/(dashboard)/dashboard/_components/super-admin-dashboard.tsx
  - src/app/(dashboard)/dashboard/_components/hr-admin-dashboard.tsx
  - src/app/(dashboard)/dashboard/_components/manager-dashboard.tsx
  - src/app/(dashboard)/dashboard/_components/employee-dashboard.tsx
  - src/lib/services/dashboard.service.ts
  - prisma/seed.ts
autonomous: false

must_haves:
  truths:
    - "HR Admin sees dashboard with headcount, pending leave, open vacancies, payroll status widgets (DASH-01)"
    - "Manager sees dashboard with today's team attendance and pending leave requests (DASH-01)"
    - "Employee sees dashboard with today's attendance status and leave balance summary (DASH-01)"
    - "Super Admin sees the same dashboard as HR Admin (shared core metrics)"
    - "Each role sees ONLY the widgets relevant to them (DASH-01)"
    - "All widgets show placeholder '0' values in Phase 1 (CONTEXT.md)"
    - "Seeded test users exist for all 4 roles to verify role-specific dashboards"
  artifacts:
    - path: "src/app/(dashboard)/dashboard/page.tsx"
      provides: "Dashboard page that switches on user role"
      contains: "role"
    - path: "src/app/(dashboard)/dashboard/_components/hr-admin-dashboard.tsx"
      provides: "HR Admin dashboard with 4 stat cards"
      contains: "StatCard"
    - path: "src/app/(dashboard)/dashboard/_components/manager-dashboard.tsx"
      provides: "Manager dashboard with team-oriented widgets"
      contains: "StatCard"
    - path: "src/app/(dashboard)/dashboard/_components/employee-dashboard.tsx"
      provides: "Employee dashboard with personal stat widgets"
      contains: "StatCard"
    - path: "src/lib/services/dashboard.service.ts"
      provides: "Dashboard data fetching service"
      exports: ["getDashboardData"]
    - path: "prisma/seed.ts"
      provides: "Seed data for all roles plus sample master data"
      contains: "MANAGER|EMPLOYEE|HR_ADMIN"
  key_links:
    - from: "src/app/(dashboard)/dashboard/page.tsx"
      to: "src/lib/auth.ts"
      via: "auth() to get session role"
      pattern: "auth\\(\\)|session\\.user\\.role"
    - from: "src/app/(dashboard)/dashboard/page.tsx"
      to: "dashboard components"
      via: "Switch on role to render appropriate dashboard"
      pattern: "switch|SUPER_ADMIN|HR_ADMIN|MANAGER|EMPLOYEE"
---

<objective>
Build role-specific dashboard views and seed test data for all 4 roles. Each role sees a different set of summary widgets. All values are placeholder zeros in Phase 1.

Purpose: DASH-01 requires each role to see role-appropriate dashboard content. This also validates that the entire auth + layout + role system works end-to-end. Seed data for all roles enables manual testing.

Output: Role-specific dashboard components, dashboard data service, and comprehensive seed data.
</objective>

<execution_context>
@C:\Users\farre\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\farre\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/01-foundation/01-RESEARCH.md
@.planning/phases/01-foundation/01-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create dashboard service and role-specific dashboard components</name>
  <files>
    src/lib/services/dashboard.service.ts
    src/app/(dashboard)/dashboard/page.tsx
    src/app/(dashboard)/dashboard/_components/super-admin-dashboard.tsx
    src/app/(dashboard)/dashboard/_components/hr-admin-dashboard.tsx
    src/app/(dashboard)/dashboard/_components/manager-dashboard.tsx
    src/app/(dashboard)/dashboard/_components/employee-dashboard.tsx
  </files>
  <action>
    1. Create `src/lib/services/dashboard.service.ts`:
    - `getDashboardData(role, userId)`: Returns dashboard data based on role.
    - For Phase 1, this returns REAL counts where possible and placeholder zeros otherwise:
      - `totalEmployees`: Count of active users (real data from DB)
      - `totalDepartments`: Count of active departments (real data)
      - `totalPositions`: Count of active positions (real data)
      - `pendingLeaveRequests`: 0 (placeholder, Phase 3)
      - `openVacancies`: 0 (placeholder, Phase 5)
      - `payrollStatus`: "Belum diproses" (placeholder, Phase 4)
      - `todayAttendance`: 0 (placeholder, Phase 3)
      - `leaveBalance`: 0 (placeholder, Phase 3)

    2. Update `src/app/(dashboard)/dashboard/page.tsx` -- Server Component:
    - Call `auth()` to get session with role
    - Call `getDashboardData(session.user.role, session.user.id)`
    - Switch on `session.user.role` to render the appropriate dashboard component:
      - SUPER_ADMIN -> SuperAdminDashboard
      - HR_ADMIN -> HRAdminDashboard
      - MANAGER -> ManagerDashboard
      - EMPLOYEE -> EmployeeDashboard
    - Page title: "Dashboard"
    - Greeting: "Selamat datang, {name}" (Welcome, {name})

    3. Create `src/app/(dashboard)/dashboard/_components/hr-admin-dashboard.tsx`:
    - 4 stat cards in a grid row (CONTEXT.md: "headcount, pending leave approvals, open vacancies, payroll status"):
      - "Total Karyawan" (Headcount): icon Users, value from data
      - "Pengajuan Cuti Menunggu" (Pending Leave): icon Clock, value 0 (placeholder)
      - "Lowongan Terbuka" (Open Vacancies): icon Briefcase, value 0 (placeholder)
      - "Status Penggajian" (Payroll Status): icon DollarSign, value "Belum diproses" (placeholder)
    - Grid layout: 4 columns on lg, 2 on md, 1 on sm

    4. Create `src/app/(dashboard)/dashboard/_components/super-admin-dashboard.tsx`:
    - Same 4 stat cards as HR Admin (CONTEXT.md: "Super Admin and HR Admin share core metrics")
    - Additional stat cards below:
      - "Total Departemen" (Departments): icon Building2, value from data
      - "Total Jabatan" (Positions): icon Award, value from data
    - SUPER_ADMIN is the system administrator, so they see organizational structure metrics too

    5. Create `src/app/(dashboard)/dashboard/_components/manager-dashboard.tsx`:
    - CONTEXT.md: "Manager: today's team attendance, pending leave requests"
    - 2 stat cards:
      - "Kehadiran Tim Hari Ini" (Today's Team Attendance): icon UserCheck, value 0 (placeholder, Phase 3)
      - "Pengajuan Cuti Menunggu" (Pending Leave Requests): icon Calendar, value 0 (placeholder, Phase 3)
    - Below: placeholder card "Data kehadiran tim akan tersedia setelah modul absensi aktif" (Team attendance data will be available after the attendance module is active)

    6. Create `src/app/(dashboard)/dashboard/_components/employee-dashboard.tsx`:
    - CONTEXT.md: "Employee: today's attendance status, leave balance summary"
    - From CONTEXT.md additional context: "remaining leave balance, latest payslip link, attendance summary"
    - 3 stat cards:
      - "Status Kehadiran Hari Ini" (Today's Attendance): icon Clock, value "Belum Absen" (Not clocked in, placeholder)
      - "Sisa Cuti Tahunan" (Remaining Annual Leave): icon Calendar, value 0 (placeholder)
      - "Slip Gaji Terakhir" (Latest Payslip): icon FileText, value "Belum tersedia" (Not available, placeholder)

    ALL stat cards use the shared StatCard component from Plan 03.
    ALL labels in Indonesian.
    ALL placeholder values show "0" or descriptive text (not blank).
  </action>
  <verify>
    - Login as Super Admin -> see 6 stat cards (4 shared + 2 organizational)
    - Total Karyawan shows real count from database
    - Total Departemen and Total Jabatan show real counts
    - Placeholder widgets show 0 or descriptive placeholder text
    - Layout is responsive (4 columns -> 2 -> 1 on smaller screens)
  </verify>
  <done>
    Role-specific dashboards created for all 4 roles. Dashboard service provides real data where available and placeholders otherwise. DASH-01 structure complete.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create comprehensive seed data for all roles and master data</name>
  <files>
    prisma/seed.ts
  </files>
  <action>
    Update `prisma/seed.ts` to create comprehensive test data. The seed must be IDEMPOTENT (use upsert or check-before-create).

    Seed data:

    1. Users (4 users, one per role):
    - Super Admin: name "Super Admin", email "admin@ptsan.co.id", password "Admin123!", role SUPER_ADMIN
    - HR Admin: name "Dewi Lestari", email "dewi.hr@ptsan.co.id", password "Admin123!", role HR_ADMIN
    - Manager: name "Budi Santoso", email "budi.mgr@ptsan.co.id", password "Admin123!", role MANAGER
    - Employee: name "Rina Wulandari", email "rina@ptsan.co.id", password "Admin123!", role EMPLOYEE

    2. Departments (3 departments):
    - "Penagihan" (Collection) - description "Departemen penagihan dan collection management"
    - "Keuangan" (Finance) - description "Departemen keuangan dan akuntansi"
    - "SDM" (HR) - description "Departemen sumber daya manusia"

    3. Positions (5 positions linked to departments):
    - "Kepala Penagihan" -> Penagihan
    - "Staff Penagihan" -> Penagihan
    - "Kepala Keuangan" -> Keuangan
    - "Staff Keuangan" -> Keuangan
    - "Staff SDM" -> SDM

    4. Office Locations (2 locations):
    - "Kantor Pusat Jakarta":
      - Address: "Jl. Sudirman No. 123, Jakarta Selatan"
      - AllowedIPs: ["192.168.1.0/24"]
      - Latitude: -6.2088, Longitude: 106.8456, Radius: 200
    - "Kantor Cabang Bekasi":
      - Address: "Jl. Ahmad Yani No. 45, Bekasi"
      - AllowedIPs: ["10.0.0.0/24"]
      - Latitude: -6.2383, Longitude: 106.9756, Radius: 150

    5. Leave Types (4 Indonesian statutory leave types):
    - "Cuti Tahunan" (Annual Leave): quota 12, paid true, no gender restriction
    - "Cuti Sakit" (Sick Leave): quota 14, paid true, no gender restriction
    - "Cuti Melahirkan" (Maternity Leave): quota 90, paid true, genderRestriction "FEMALE"
    - "Cuti Ayah" (Paternity Leave): quota 2, paid true, genderRestriction "MALE"

    Hash all passwords with bcryptjs (12 rounds).
    Use `prisma.$transaction` for atomicity.
    Print summary at end: "Seed complete: X users, X departments, X positions, X locations, X leave types"
  </action>
  <verify>
    - `npx prisma db seed` runs successfully
    - Running seed again does not create duplicates (idempotent)
    - Prisma Studio shows all seeded data
    - Can log in as each of the 4 seeded users
    - Each user sees their role-specific dashboard
  </verify>
  <done>
    Comprehensive seed data covering all 4 roles and all master data types. Ready for end-to-end testing of the complete Phase 1.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Complete Phase 1 Foundation: Authentication, user management, master data (departments, positions, office locations, leave types), audit logging, and role-specific dashboards.
  </what-built>
  <how-to-verify>
    Run `npx prisma db seed` to ensure test data exists, then `npm run dev`.

    1. Login as Super Admin (admin@ptsan.co.id / Admin123!):
       - Dashboard shows 6 stat cards with real counts for karyawan/departemen/jabatan
       - Navigate to "Pengguna" -> create a new user, edit a user, deactivate/reactivate
       - Navigate to "Data Master" -> test all 4 tabs (departments, positions, locations, leave types) - create, edit, delete
       - Navigate to "Log Audit" -> see entries from your actions, try filters

    2. Login as HR Admin (dewi.hr@ptsan.co.id / Admin123!):
       - Dashboard shows 4 stat cards (headcount, pending leave, vacancies, payroll)
       - Sidebar should NOT show "Pengguna", "Data Master", or "Log Audit"

    3. Login as Manager (budi.mgr@ptsan.co.id / Admin123!):
       - Dashboard shows 2 stat cards (team attendance, pending leave)
       - Sidebar shows only "Dashboard"

    4. Login as Employee (rina@ptsan.co.id / Admin123!):
       - Dashboard shows 3 stat cards (attendance, leave balance, payslip)
       - Sidebar shows only "Dashboard"

    5. Test logout from any role's header dropdown
    6. Test session persistence (refresh browser while logged in)
    7. Verify all UI labels are in Indonesian
  </how-to-verify>
  <resume-signal>Type "approved" if all 5 success criteria pass, or describe specific issues to fix.</resume-signal>
</task>

</tasks>

<verification>
- DASH-01: Each role sees role-appropriate dashboard
- Super Admin/HR Admin share core metrics (headcount, pending leave, vacancies, payroll)
- Manager sees team attendance + pending leave
- Employee sees personal attendance + leave balance + payslip
- All placeholder values show meaningful zeros, not blank
- Seed data covers all 4 roles and all master data types
- End-to-end flow works from login through all features
</verification>

<success_criteria>
Phase 1 complete when ALL 5 success criteria from the roadmap are met:

1. HR Admin can log in and see role-appropriate dashboard with summary widgets
2. Super Admin can create, deactivate, reactivate users, and change roles
3. Super Admin can create departments, positions, office locations, and leave types via tabbed master data page
4. Every create/update/delete action is recorded in audit log with filters
5. Manager and Employee each see role-specific dashboards with relevant-only widgets
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-09-SUMMARY.md`
</output>
